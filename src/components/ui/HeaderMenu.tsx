import React from 'react';

const HeaderMenus = ({ title }: any) => {
  return (
    <div>
      <h1 className="font-bold text-4xl text-text-primary text-center py-12">{title}</h1>
    </div>
  );
};

export default HeaderMenus;
